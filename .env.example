# PostgreSQL Database Configuration
# Copy this file to .env and update with your database settings

# Default configuration (recommended)
DB_HOST=localhost
DB_PORT=5432
DB_USER=currency_app
DB_PASSWORD=currency123
DB_NAME=currency_tracker
DB_SSL_MODE=disable

# Alternative configuration for default postgres user:
# DB_USER=postgres
# DB_PASSWORD=your_postgres_password
